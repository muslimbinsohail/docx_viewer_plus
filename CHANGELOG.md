## 1.0.0

* Initial release of `docx_viewer_plus`.
* Added support for parsing and viewing DOCX files.
* Added rich text editor support for modification of documents.
* Added API `DocxService` to load, parse, and save DOCX formats locally.
* Out-of-the-box UI component `DocxViewerScreen` with customizable attributes via `DocxViewerConfig`.
